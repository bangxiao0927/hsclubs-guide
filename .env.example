# Leave empty to use same-origin /api requests. Local development uses MSW by default.
VITE_DIRECTORY_API_BASE_URL=

# Set to false only when intentionally connecting to a local or staging directory API.
VITE_USE_MSW=true
